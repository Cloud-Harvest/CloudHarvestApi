api:
  host: 0.0.0.0
  port: 8000

cache:
  connection:
    host: localhost
    port: 27017
    username:     # intentionally null
    password:     # intentionally null

  # base.py backend database indexes
  indexes:
    harvest:  &harvest_indexes
      api_nodes:
        - hostname
        - end
      pstar:
        - Platform
        - Service
        - Type
        - Account
        - Region
        - Records
        - Errors
        - keys:
            - field: Platform
            - field: Service
            - field: Type
            - field: Account
            - field: Region
          options:
            name: unique_pstar_idx
            comment: unique identifier for pstar records
            unique: true
      meta:
        - [Collection, CollectionId]
        - keys:
            - field: Harvest.Module.UniqueFilter
          options:
            unique: true
    test:
      <<: *harvest_indexes

logging:
  level: debug
  location: ./.harvest/api/logs/

# retrieves harvest modules from remote git repositories
modules:
  path: ./.harvest/api/plugins
  repos:
    - source: git@github.com:Cloud-Harvest/plugin-aws.git
      label: main
#    - https://github.com/Cloud-Harvest/module-aws.git
